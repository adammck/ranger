{
    "version": "0.2.0",
    "configurations": [
        {
            "type":"go",
            "name": "Launch Controller",
            "request": "launch",
            "program": "${workspaceFolder}/cmd/rangerd",
            "args": ["-addr", ":5200"],
        }
    ]
}